

Why are ID selectors a bad idea? 
    Classes can be reused, but an element can only have one ID, meaning using IDs limits your options.


What selectors should you prefer when working with CSS?
    It's easiest and cleanest to work with semantic selectors in CSS.